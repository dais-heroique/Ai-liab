(()=>{const nav=document.querySelector('.nav'),menu=document.querySelector('.menu');if(menu){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));if(open&&!nav.querySelector('.mobile-nav'))nav.insertAdjacentHTML('beforeend','<div class="mobile-nav"><a href="#produit">Produit</a><a href="#securite">Sécurité</a><a href="#developers">Développeurs</a><a href="/pricing">Tarifs</a><a href="/login">Se connecter</a></div>');else if(!open)nav.querySelector('.mobile-nav')?.remove()})}document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>{nav?.classList.remove('open');menu?.setAttribute('aria-expanded','false');nav?.querySelector('.mobile-nav')?.remove()}));const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in-view');io.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.body.classList.add('loaded')}else document.querySelectorAll('.reveal').forEach(el=>el.classList.add('in-view'));const flow=document.querySelector('[data-flow]');if(flow&&!reduce){const nodes=[...flow.querySelectorAll('.node')];let i=1;window.setInterval(()=>{nodes.forEach(n=>n.classList.remove('active'));nodes[i%nodes.length].classList.add('active');i++},2200)}document.querySelectorAll('.magnetic').forEach(el=>{if(reduce)return;let raf=0,x=0,y=0;el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();x=(e.clientX-r.left-r.width/2)*.07;y=(e.clientY-r.top-r.height/2)*.07;if(!raf)raf=requestAnimationFrame(()=>{el.style.transform=`translate3d(${x}px,${y}px,0)`;raf=0})});el.addEventListener('pointerleave',()=>{el.style.transform='';if(raf){cancelAnimationFrame(raf);raf=0}})});const tilt=document.querySelector('.tilt');if(tilt&&!reduce){let raf=0;tilt.addEventListener('pointermove',e=>{const r=tilt.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;if(!raf)raf=requestAnimationFrame(()=>{tilt.style.transform=`perspective(900px) rotateY(${x*4}deg) rotateX(${y*-4}deg)`;raf=0})});tilt.addEventListener('pointerleave',()=>tilt.style.transform='')}const cards=[...document.querySelectorAll('.capabilities article')];cards.forEach(card=>card.addEventListener('pointerenter',()=>{if(!reduce)card.style.transform='translateY(-3px)'}));cards.forEach(card=>card.addEventListener('pointerleave',()=>card.style.transform=''));const demo=document.querySelector('[data-verification-demo]');const run=demo?.querySelector('[data-run-demo]');if(demo&&run){let timer;run.addEventListener('click',()=>{if(timer)clearTimeout(timer);demo.classList.remove('running','done');const status=demo.querySelector('[data-demo-status]'),result=demo.querySelector('[data-demo-result]');result.textContent='EN ATTENTE';run.disabled=true;run.setAttribute('aria-busy','true');status.textContent='Analyse de la demande…';demo.classList.add('running');timer=setTimeout(()=>{status.textContent='Reformulation produite — comparaison en cours…';},700);timer=setTimeout(()=>{status.textContent='Vérification terminée — la démo illustre le contrôle YES / NO.';result.textContent='YES';demo.classList.add('done');run.disabled=false;run.removeAttribute('aria-busy')},1800)}}})();